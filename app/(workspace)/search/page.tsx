import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Search | EduEssentials",
  description: "Search assignments, classes, notes, course files, and events.",
};

export default function SearchPage() {
  return null;
}
